# Application Name
spring.application.name=backend

# Exclude Default DataSource Configuration
spring.autoconfigure.exclude=org.springframework.boot.autoconfigure.jdbc.DataSourceAutoConfiguration

# Server Port (Optional: Change if needed)
server.port=8080

# Logging Level (Optional: Adjust for debugging)
logging.level.org.springframework=INFO
logging.level.com.mycreditscore=DEBUG

# Firebase (Add any necessary custom properties if needed in future)
